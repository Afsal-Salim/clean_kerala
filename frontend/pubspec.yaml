name: make_kerala_clean
description: Make Kerala Clean — community waste reporting platform
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.2
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.4
  intl: ^0.20.1
  google_fonts: ^6.2.1
  image_picker: ^1.1.2
  google_mlkit_image_labeling: ^0.12.0
  geolocator: ^13.0.2
  permission_handler: ^11.3.1
  shared_preferences: ^2.3.4
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
